/**
 * Ключ в localStorage, где лежит выбранный человеком язык.
 *
 * Пишут его два места — переключатель языка (человек выбрал явно) и подсказка
 * (человек её закрыл, то есть текущий язык его устраивает). Читает подсказка.
 * Поэтому ключ вынесен сюда: два инлайновых скрипта не могут делить константу
 * иначе, а разъехавшиеся строки сломались бы молча.
 *
 * Это настройка, а не слежка: значение — код языка, оно не покидает браузер
 * и никуда не отправляется.
 */
export const LANG_PREF_KEY = 'dcss3d.lang';
